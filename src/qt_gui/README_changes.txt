本次修改内容（2026-05-11）：

1. 登录页样式统一
   - 重写 res/qss/style-1.qss ~ style-4.qss。
   - 粉色、黄色、浅紫、青绿四套主题的 LOGIN 标题、账号输入框、密码输入框、图标、按钮圆角和输入框边框保持统一结构。
   - 保留各主题的主色、悬停色和背景图，使不同主题视觉一致但色彩区分明确。

2. 登录页标题艺术化
   - 将登录页顶部标题设置为“欢迎使用康复指导系统”。
   - 通过 QSS 对 #label_title 添加大字号、加粗、半透明圆角底板和渐变文字色，提高视觉吸引力。

3. 修复二级页面导航文字重复
   - 修改 widgets/navibar.cpp。
   - 原来的按钮文字由“图标文字 + 换行 + 同名文字”组成，导致“首页\n首页”“评估\n评估”等重复显示。
   - 现在每个导航按钮只显示一次：“首页 / 评估 / 训练 / 记录 / 医疗建议 / 设置”。

4. 多屏幕自适应
   - 登录页：将原来固定几何位置的 layoutWidget 和 layoutWidgetLoginFields 放入真实布局中，解决大屏偏左、小屏裁切、拉伸错位等问题。
   - 主界面：取消固定 1024x600 尺寸，按当前屏幕可用区域设置初始窗口大小；小屏自动最大化。
   - 开启高 DPI 缩放支持，减少 Win11 不同缩放比例下的字体和控件比例问题。
   - 对首页按钮、导航栏、训练动作卡片、雷达图和仪表盘等控件做了最小尺寸/弹性布局调整。

主要修改文件：
- main.cpp
- widget.h
- widget.cpp
- widget.ui
- mainwindow.cpp
- widgets/navibar.cpp
- widgets/actioncard.cpp
- widgets/arcgauge.cpp
- widgets/radarchart.cpp
- pages/homepage.cpp
- pages/assessmentpage.cpp
- pages/trainingpage.cpp
- pages/settingspage.cpp
- res/qss/style-1.qss
- res/qss/style-2.qss
- res/qss/style-3.qss
- res/qss/style-4.qss

追加修改内容（本次）：

1. 一级登录页右侧图片比例修正
   - 修改 widget.cpp 中的响应式布局参数。
   - 将左侧登录框 frame_login 与右侧图片框 frame_pic 设置为相同的最小尺寸和 1:1 拉伸比例。
   - 图片按 KeepAspectRatioByExpanding 方式填充右侧框架，避免图片过小或与左侧框架比例不一致。

2. 黄色、青绿色主题标题对比度增强
   - 修改 res/qss/style-2.qss 与 res/qss/style-4.qss。
   - 将顶部标题和 LOGIN 标题改为更深、更清晰的高对比颜色。
   - 修复黄色、青绿色主题按钮按下状态中文字颜色不明显的问题。

3. 记录页改为“综合训练记录 + 第三级详情页”
   - 重写 pages/recordspage.h 和 pages/recordspage.cpp。
   - 二级“记录”页面现在只显示一条大的“训练综合得分记录”，其中包含第1次、第2次等所有训练的综合得分。
   - 记录页不显示任何时间信息，只显示第几次训练、训练动作、级别、完成度和综合得分。
   - 点击任意“综合得分”按钮后，切换到记录页内部的第三级详情页面。
   - 第三级详情页包含综合得分、训练级别、完成度、动作名称、关节活动度、动作平滑度、抖动控制、左右对称、节奏控制、疲劳稳定和训练建议。

2026-05-11 追加修改 2：
1. 登录页默认主题改为蓝色/浅紫风格，即程序启动后自动加载 style-3.qss 与 3.png。
2. 扩大账号、密码输入框和内部文本区宽度，压缩左右固定空白，降低输入提示字体占用，避免“最多8位/有效数字”等提示文字在高 DPI 或不同字体缩放下被截断。
3. 首页仪表盘适度放大；每次进入首页都会从 0 动画加载到最后一次训练综合得分。
4. 首页仪表盘分数与记录页最后一条训练记录同步；删除记录后再次进入首页会自动显示新的最后一次训练得分，若无记录则显示未评估。
5. 记录页增加“选择”“全选/取消”“删除选中历史记录”功能，可勾选并删除历史训练记录；删除后不会因刷新记录页而恢复。


【本次字体统一修改】
1. 已将程序入口 QApplication 全局字体改为 Microsoft YaHei（微软雅黑）。
2. 已将登录页 QSS 中原 Arial、Microsoft YaHei UI、SimHei 等显式字体统一为 Microsoft YaHei。
3. 已将自绘仪表盘、雷达图中的 QFont 显式字体统一为 Microsoft YaHei。
4. 已将 widget.ui 中按钮、标题、登录控件等字体 family 统一为 Microsoft YaHei。

2026-05-12 登录页输入框遮挡修复：
- 调整登录面板与背景容器的响应式内边距，避免小窗口/DPI 缩放时可用宽度不足。
- 将账号、密码输入框改为弹性宽度，移除导致裁切的固定最小宽度。
- 将账号、密码输入框高度统一为 54px，LineEdit 文本区高度统一为 38px，保证图标、占位文字和输入文字无遮挡。
- 四套 QSS 主题同步增加输入框 min-height 与文本内边距。

2026-05-16 一键进入修改：
1. 将一级登录页改为“一键进入”入口，按钮文字改为“开始使用”。
2. 点击“开始使用”后直接创建并显示 MainWindow 二级功能页面，不再读取、校验或保存账号密码。
3. 删除 widget.h / widget.cpp 中原账号注册、账号存在判断、密码哈希、本地 accounts.ini 存储和密码校验相关逻辑。
4. 隐藏账号输入框、密码输入框、注册按钮和原 LOGIN 标题，保留四套背景风格选择；进入二级页面后继续沿用当前选择的主题。
5. 四套 QSS 增加一键入口面板样式，使新入口页保持原有主题风格。

主要修改文件：
- widget.h
- widget.cpp
- widget.ui
- res/qss/style-1.qss
- res/qss/style-2.qss
- res/qss/style-3.qss
- res/qss/style-4.qss

2026-05-23 一级登录页文字删除：
1. 删除入口页中“一键进入康复系统”“无需账号、密码和注册...”和“可先在左上角选择背景风格...”三处说明文字。
2. 保留原进入按钮和主题选择功能。

7. 二级页面新增“医疗建议”入口：
   - 首页快捷按钮区新增“医疗建议”按钮，与“开始评估 / 今日训练 / 训练记录 / 系统设置”保持同级样式。
   - 底部导航栏在“记录”之后新增“医疗建议”按钮。
   - 点击后进入新增三级页面 MedicalAdvicePage，页面按当前需求保持留白。

2026-05-31 医疗建议三级页面增强：
- 医疗建议页接收最近一次评估结果，显示综合得分、总体建议和六维雷达图。
- 添加六维雷达图，修复雷达图中文维度名与评分引擎英文 key 不一致导致数据不显示的问题。
- 根据 Word 文档内容补充六个维度的评分状态、低分原因、训练建议、医疗建议和建议药物方向。
- 导入康复动作图片和药物图片资源，并在对应维度卡片中展示参考图片。
- 评分权重调整为抬举幅度 30%、运动平滑度 25%、震颤程度 20%、双侧对称性 15%、运动速度 5%、运动耐力 5%。

2026-05-31 医疗建议记录化修改：
1. 医疗建议三级页面改为“记录列表 + 详情页”结构，每完成一次评估自动新增一条医疗建议记录。
2. 记录列表每条显示“第几次评估医疗建议”和“综合得分”，点击“查看建议”进入对应详情。
3. 详情页保留本次评估综合得分、等级、总体建议、六维雷达图，并按六个维度分别展示得分、训练建议、医疗建议与建议药物方向。
4. 已删除医疗建议页面中所有“评价重点”展示内容，仅保留评估姿势、低分可能原因、训练建议和医疗/用药方向。

2026-05-31 医疗建议页面二次调整：
1. 删除总体评分卡片中的“总体用药方向”展示，只保留综合得分、总体评分建议和六维雷达图。
2. 第几次评估详情页新增“六个维度对应建议”入口，每个维度显示单独得分、状态和简要建议。
3. 点击任一维度的“具体分析”按钮后进入下一级独立分析页面，展示该维度的具体分析、评分进度、评估姿势、低分可能原因、训练建议和医疗/建议药物方向。
4. 独立分析页面中的动作图片和药物图片已放大显示，提升图片在单维评估页面中的占比。

2026-05-31 数据持久化修改：
- 新增 models/datastorage.h/.cpp，用于统一读写本地 JSON 数据。
- 最新一次六维评估结果会保存到 latest_assessment.json，重新打开程序后评估页会自动读取并显示上一次评估结果。
- 训练记录页数据会保存到 training_records.json，新增记录和删除记录后都会同步写入本地。
- 医疗建议记录会保存到 medical_advice_records.json，重新打开程序后仍可查看每次评估对应的医疗建议记录和六维独立分析。
- 数据默认存储在 Qt 的 AppDataLocation 目录；若系统无法获取该目录，则存储到用户主目录下 .rehab_coach 文件夹。

本次修改：L1/L2/L3/L4 一体化训练流程
- 取消训练页中每个级别下的单独动作卡片入口，例如“坐姿肩关节前屈上举”等不再作为独立训练入口显示。
- L1、L2、L3、L4 现在分别作为一个综合训练包；选择级别后可直接点击“开始训练”。
- 点击开始后，系统会按该级别下的四个功能块顺序自动进入训练，并在界面上显示当前训练中的功能块、进度和完成状态。
- 训练完成或点击“停止并保存”后，记录页写入对应 L1/L2/L3/L4 综合训练记录。
- 训练记录列表显示“第几次 + 综合训练包 + 综合得分”；点击综合得分后进入详情页。
- 详情页由原来的六维评分展示改为四个功能块分项得分展示；已取消旧版单动作/六维度训练记录兼容。
- training_records.json 升级到 version=3，仅保存一体化训练记录，必须包含 blockNames 与 blockScores 四个功能块评分字段。
